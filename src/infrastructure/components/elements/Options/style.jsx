import { makeStyles } from "@material-ui/core";

export const useStylesOptions = makeStyles((theme) => ({
  container: {
    display: "flex",
    justifyContent: "space-between",
    alignItems: "center",
    margin: "10px 0px",
  },
}));
